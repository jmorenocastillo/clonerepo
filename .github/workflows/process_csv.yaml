name: Terraform - JENKINS EXACTO

on:
  push:
    branches: [ main ]

jobs:
  terraform:
    environment: production  # ← BOTÓN AQUÍ
    runs-on: ubuntu-latest
    
    steps:
    - name: Plan
      run: |
        echo "🗺️ PLAN: 2 recursos a crear"
        echo "## Plan" >> $GITHUB_STEP_SUMMARY
        echo "- Server: web-01" >> $GITHUB_STEP_SUMMARY
        echo "- DB: db-01" >> $GITHUB_STEP_SUMMARY
    
    - name: Apply
      run: echo "✅ APPLY COMPLETED!"