name: Terraform Dummy - APPROVE EN UI

on:
  push:
    branches: [ main ]

jobs:
  terraform:
    # ⏸️ PAUSA AUTOMÁTICA CON BOTÓN AQUÍ
    environment: production
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout
      uses: actions/checkout@v4
    
    - name: Plan
      run: |
        echo "=== 🗺️ TERRAFORM PLAN ==="
        echo "## Plan" >> $GITHUB_STEP_SUMMARY
        echo "- Server: web-01 (CREATE)" >> $GITHUB_STEP_SUMMARY
        echo "- DB: db-01 (CREATE)" >> $GITHUB_STEP_SUMMARY
        echo "**2 recursos a crear**" >> $GITHUB_STEP_SUMMARY
    
    # ⏸️ SE CONGELA AQUÍ - BOTÓN APARECE AUTOMÁTICO
    
    - name: Apply
      run: |
        echo "=== ✅ APPLY COMPLETED ==="
        echo "**Production Deployed!** 🎉"