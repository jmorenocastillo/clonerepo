# Este es un WORKFLOW REUTILIZABLE
name: Reusable Workflow

on:
  workflow_call:
    inputs:
      caller_job:
        type: string
        required: true

jobs:
  execute:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "This is executed by the caller job: ${{ inputs.caller_job }}"